        {building.cost && (
          <span>
            {" ("}
            {Object.entries(building.cost)
              .map(([resource, cost]) => `${resource}: ${cost}`)
              .join(", ")}
            {")"}
          </span>
        )}